# Environment variables for production deployment
# Copy this file to .env.production and fill in the actual values

# GitHub API Configuration
# Generate a Personal Access Token at https://github.com/settings/tokens
# Required scopes: repo, actions:read
GITHUB_TOKEN=ghp_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# GitHub Repositories to Monitor
# Comma-separated list of owner/repo pairs
# Example: LanNguyenSi/telerithm,LanNguyenSi/agent-ops-dashboard,LanNguyenSi/triologue
GITHUB_REPOS=

# Application Configuration
NODE_ENV=production

# Optional: Custom Port (default: 3000)
# PORT=3000

# ── Gateway Auth ─────────────────────────────────────────────
# Shared bearer token required by the gateway for all non-/health routes.
# When unset, the gateway loud-fails authenticated routes with HTTP 503.
# Generate a random one, e.g.: openssl rand -hex 32
GATEWAY_TOKEN=

# Comma-separated CORS allowlist for the gateway. Defaults to
# `https://ops.opentriologue.ai` in production. Add origins for any
# additional UIs that must talk to the gateway directly from a browser.
GATEWAY_ALLOWED_ORIGINS=https://ops.opentriologue.ai
